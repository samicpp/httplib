[package]
name = "http"
version = "1.1.7"
edition = "2024"

[dependencies]
# quinn = { path = "../quinn/quinn" }
# httprs_core = { path = "../core" }
tokio = { version = "=1.49.0", features = ["full"] }
